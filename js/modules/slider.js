export function initSlider() {
    // Slider logic is now handled by CSS animations in style.css
    // This ensures smoother performance and better control over speed
    // The .slider class uses @keyframes scroll
}
